<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context="com.dwwen.post.PostActivity">

    <item android:id="@+id/action_open_web"
        android:icon="@drawable/ic_open_in_browser"
        android:title="Read"
        android:showAsAction="always"
        app:showAsAction="always"/>

    <item android:id="@+id/action_like"
        android:icon="@drawable/ic_action_like"
        android:title="@string/action_like"
        android:showAsAction="always"
        app:showAsAction="always"/>

    <item android:id="@+id/action_fav"
        android:icon="@drawable/ic_action_fav"
        android:title="@string/action_fav"
        android:showAsAction="always"
        app:showAsAction="always"/>

    <item android:id="@+id/action_share"
        android:title="Share"
        app:showAsAction="always"
        android:showAsAction="always"
        app:actionProviderClass="android.support.v7.widget.ShareActionProvider"
        />
</menu>
